{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.77systems-ai/receipts",
  "description": "Verification for every AI agent: destination receipts, package binding, and retry laws.",
  "repository": {
    "url": "https://github.com/77systems-ai/receipts",
    "source": "github"
  },
  "version": "0.4.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@77systems/receipts-mcp",
      "version": "0.4.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "RECEIPTS_AUDIT_PATH",
          "description": "Local append-only audit JSONL path. Defaults to .receipts/audit.jsonl in the process working directory.",
          "format": "filepath",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "RECEIPTS_GITHUB_REPO",
          "description": "Exact GitHub owner/repo for independent issue read-back. Omit for cooperative-only use.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "GITHUB_TOKEN",
          "description": "Local GitHub token authorized to read the configured repository. Sent only to the GitHub API for authentication.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "RECEIPTS_POLICY_PATH",
          "description": "Path to a local JSON WritePolicy applied at claim and dispatch. Host-owned startup configuration; no tool call can change it. Omit to permit registered surfaces.",
          "format": "filepath",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "RECEIPTS_CLAIM_TTL_MS",
          "description": "Positive integer TTL in milliseconds for unused claims. Dispatched claims never expire into another write. Defaults to the core registry default.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "RECEIPTS_SIGNING_KEY_PATH",
          "description": "Path to a local 0600 PEM PKCS8 Ed25519 private key whose contents never leave the machine. Enables receipts.sign and receipts.badge; omit to keep signing disabled.",
          "format": "filepath",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "RECEIPTS_FILE_ROOTS",
          "description": "Absolute directories the file-write connector may read back and receipts.prepare may claim staged files from, separated by the platform path delimiter. Required for either.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "RECEIPTS_FILE_ACCOUNT",
          "description": "Destination account label for file-write receipts. Defaults to local:file.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "RECEIPTS_GMAIL_ACCOUNT",
          "description": "Mailbox label for Gmail send receipts; must match the claimed destinationAccount. Requires RECEIPTS_GMAIL_TOKEN.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "RECEIPTS_GMAIL_TOKEN",
          "description": "Local Google OAuth access token able to read the mailbox. Sent only to gmail.googleapis.com; never audited.",
          "isRequired": false,
          "isSecret": true
        }
      ]
    }
  ]
}
